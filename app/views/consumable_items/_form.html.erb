<%= form_with model: @consumable_item, local: true do |form| %>

  <% if @consumable_item.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@consumable_item.errors.count, "error") %> prohibited
        this consumable_item from being saved:
      </h2>
      <ul>
        <% @consumable_item.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <div class="field">
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :consumable_item_type %><br>
	<%= form.collection_select(:consumable_item_type_id, ConsumableItemType.all, :id, :name ) %>
  </div>

  <div class="field">
    <%= form.label :approx_calories %><br>
    <%= form.text_field :approx_calories %>
  </div>
 
  <div class="field">
    <%= form.label :notes %><br>
    <%= form.text_area :notes %>
  </div>
 
  <div class="actions">
    <%= form.submit %>
  </div>
 
<% end %>