<%= form_with(model: symptom_type, local: true) do |form| %>
  <% if symptom_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(symptom_type.errors.count, "error") %> prohibited this symptom_type from being saved:</h2>

      <ul>
      <% symptom_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :symptom_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :symptom_type_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
